<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.comtietea.comtietea">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".SemanticFieldActivity" />
        <activity android:name=".ActionsActivity" />
        <activity android:name=".CommonWordActivity" />
        <activity android:name=".CreateSemanticFieldActivity" />
        <activity android:name=".CreateCommonWordActivity" />
        <activity android:name=".CommonWordDetailActivity"></activity>
    </application>

</manifest>